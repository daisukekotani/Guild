<div class="container">
	<div class="row">
		<div class="col-sm-９">
			<h1>声編集</h1>
				<%= form_with model:@voice, local:true do |f| %>
				<h4>内容</h4>
				<%= f.text_area :body, rows:'5', class: "form-control" %>
				<%= f.submit '投稿' %>
				<% end %>
		</div>
	</div>
</div>